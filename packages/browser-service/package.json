{
  "name": "@agentpass/browser-service",
  "version": "0.1.0",
  "type": "module",
  "description": "Browser automation service using Playwright for fallback auth",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentpass/core": "workspace:*",
    "playwright": "^1.50.1"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
